{
  "name": "Development Container",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "postCreateCommand": ". /workspaces/qupled_venv/bin/activate && \
                        pip install foga && \
                        foga install --target external-apt && \
                        foga install --target package"
}